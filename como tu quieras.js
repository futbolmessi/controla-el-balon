var xnariz=0;
var ynariz=0;

function preload() {
balonfcb=loadImage
}
function setup() {
canvas=createCanvas(399,299);
canvas.position(500,150);
video=createCapture(VIDEO);
video.hide();
 colors="";
}
function draw() {
image (video,0,0,399,299);
}

